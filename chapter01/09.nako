strは"I couldn't believe that I could actually understand what I was reading : the phenomenal power of the human mind ."
strをtypo
表示

*typo(Sを)
	retStrとは配列
	Sを「 」で区切るを反復
		wordはそれ
		lenはそれの文字数
		もし、len>=4ならば
			arrはwordの文字列分解
			#arrを表示
			arrはarrを配列シャッフル
			arrを「」で配列結合
			retStrにそれを配列追加
		違えば、
			retStrにwordを配列追加
	retStrを「 」で配列結合して戻る
	#retStrで戻る
		
