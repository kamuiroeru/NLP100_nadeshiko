カテゴリとは配列
"UK.txt"を毎行読んで反復
	行はそれをUTF8_SJIS変換
	#行を「m/:[^\\\/:\*\?\"\<\>\|]+\.[a-zA-Z]{,4}\|/g」で正規表現マッチ
	行を「m/:\w+\.\w{3}\|/g」で正規表現マッチ
	それを表示
	もし、それ <> 「」ならば
		それを表示
	#もし、((行が「(=+).+\1」に正規表現一致)＝はい)ならば
	#	行を「(=+)(.+)\1」で正規表現マッチ
	#	カテゴリに「｛抽出文字列[1]｝：｛抽出文字列[0]の文字数-1｝」を配列追加
＃カテゴリを表示
