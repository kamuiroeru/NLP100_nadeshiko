メディアファイルとは配列
"UK.txt"を毎行読んで反復
	行はそれをUTF8_SJIS変換
	# Windowsファイル名での使用禁止文字以外 ピリオド 拡張子
	# の正規表現
	# {}が変数展開でないことを示すために、『』でパターン指定する。
	行を『:([^\\\/:\*\?\"\<\>\|]+\.\w{1,4})\|』で正規表現マッチ
	もし、それ <> 「」ならば
		メディアファイルに抽出文字列[0]を配列追加
		#それを表示
メディアファイルを表示
