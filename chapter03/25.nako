テンプレートとは配列
"UK.txt"を毎行読んで反復
	行はそれをUTF8_SJIS変換
	行を「\|(.+) = (.+)」で正規表現マッチ
	もし、それ <> 「」ならば
	#もし、((行が「\|(.+) = (.+)\<?」に正規表現一致)＝はい)ならば
		#行を「(=+)(.+)\1」で正規表現マッチ
		テンプレートに「｛抽出文字列[0]｝：｛抽出文字列[1]｝」を配列追加
テンプレートを表示
